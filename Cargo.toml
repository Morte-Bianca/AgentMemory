[workspace]
members = ["chain/solana/program"]
resolver = "2"

# Deterministic release settings commonly used for verified builds
[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
